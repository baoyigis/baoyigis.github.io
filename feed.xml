<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://baoyigis.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://baoyigis.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-02-23T02:58:59+00:00</updated><id>https://baoyigis.github.io/feed.xml</id><title type="html">Yi Bao</title><subtitle>Assistant Professor at Nanjing University, focusing on urban spatiotemporal big data mining, high-resolution urban built environment research, and the interaction mechanisms between built environment and human activities. </subtitle><entry><title type="html">Lesson 0: 构建现代空间数据科学环境</title><link href="https://baoyigis.github.io/blog/2025/lesson0/" rel="alternate" type="text/html" title="Lesson 0: 构建现代空间数据科学环境"/><published>2026-02-06T02:00:00+00:00</published><updated>2026-02-06T02:00:00+00:00</updated><id>https://baoyigis.github.io/blog/2025/lesson0-environment-setup</id><content type="html" xml:base="https://baoyigis.github.io/blog/2025/lesson0/"><![CDATA[<h2 id="第一部分课前技术博客-pre-class-technical-blog">第一部分：课前技术博客 (Pre-class Technical Blog)</h2> <h3 id="1-引言工程化思维的引入">1. 引言：工程化思维的引入</h3> <p>在理论课程中，我们讨论了空间分析从定性描述向定量计算的演进。为了处理大规模、高异质性的地理数据，我们需要从图形界面（GUI）转向脚本代码（Code）模式。这不仅是为了效率（如批量处理），更是为了确保分析过程的<strong>可复现性 (Reproducibility)</strong> 与 <strong>可扩展性 (Scalability)</strong>。</p> <p>本学期我们将采用业界现代化的 Python 数据科学技术栈。</p> <h3 id="2-工具链选型">2. 工具链选型</h3> <p>我们精选了以下两款核心工具，旨在为您提供一个轻量、高效且标准化的开发环境：</p> <ul> <li> <p><strong>Visual Studio Code (VS Code)</strong></p> <ul> <li><strong>定位</strong>：目前最主流的轻量级代码编辑器（IDE）</li> <li><strong>作用</strong>：提供代码编写、调试、Jupyter Notebook 渲染及文件管理功能</li> </ul> </li> <li> <p><strong>uv</strong></p> <ul> <li><strong>定位</strong>：基于 Rust 编写的高性能 Python 包与项目管理器</li> <li><strong>优势</strong>：相比传统的 Conda，uv 的依赖解析速度极快，且能通过 <code class="language-plaintext highlighter-rouge">uv.lock</code> 文件严格锁定环境版本，彻底解决”在他电脑上能跑，在我电脑上跑不通”的环境一致性问题</li> </ul> </li> </ul> <h3 id="3-环境配置指南-configuration-guide">3. 环境配置指南 (Configuration Guide)</h3> <p>请按照以下步骤完成环境初始化。</p> <h4 id="step-1-安装基础软件">Step 1: 安装基础软件</h4> <p><strong>1. 安装 VS Code</strong></p> <p>访问 <a href="https://code.visualstudio.com/">code.visualstudio.com</a> 下载并安装。</p> <p><em>配置插件</em>：启动 VS Code，点击左侧扩展图标（Extensions），搜索并安装 <strong>Python</strong> 和 <strong>Jupyter</strong> 插件。</p> <p><strong>2. 安装 uv</strong></p> <p>打开终端（Windows 用户请打开 PowerShell，Mac 用户打开 Terminal），复制并运行以下命令：</p> <ul> <li><strong>Windows</strong>: <code class="language-plaintext highlighter-rouge">powershell -c "irm https://astral.sh/uv/install.ps1 | iex"</code></li> <li><strong>Mac/Linux</strong>: <code class="language-plaintext highlighter-rouge">curl -LsSf https://astral.sh/uv/install.sh | sh</code></li> </ul> <h4 id="step-2-初始化课程项目">Step 2: 初始化课程项目</h4> <p>在你的磁盘中新建一个文件夹（例如命名为 <code class="language-plaintext highlighter-rouge">spatial_analysis</code>），在 VS Code 中打开该文件夹，然后使用 <code class="language-plaintext highlighter-rouge">Ctrl+~</code> (Mac 为 <code class="language-plaintext highlighter-rouge">Cmd+~</code>) 唤起内置终端，执行以下命令：</p> <p><strong>1. 初始化项目环境</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv init
</code></pre></div></div> <p>此命令会创建 <code class="language-plaintext highlighter-rouge">pyproject.toml</code> 配置文件，用于记录项目依赖。</p> <p><strong>2. 创建虚拟环境</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 创建名为 .venv 的虚拟环境</span>
uv venv
</code></pre></div></div> <p><strong>3. 激活虚拟环境</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># macOS/Linux</span>
<span class="nb">source</span> .venv/bin/activate

<span class="c"># Windows</span>
.venv<span class="se">\S</span>cripts<span class="se">\a</span>ctivate
</code></pre></div></div> <p>激活后，终端提示符前会显示 <code class="language-plaintext highlighter-rouge">(.venv)</code> 标识。</p> <p><strong>4. 安装空间分析依赖栈</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv add geopandas matplotlib folium mapclassify jupyterlab ipykernel geodatasets
</code></pre></div></div> <p><strong>库说明</strong>：</p> <table> <thead> <tr> <th style="text-align: left">库名称</th> <th style="text-align: left">主要用途</th> <th style="text-align: left">核心功能</th> </tr> </thead> <tbody> <tr> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">geopandas</code></td> <td style="text-align: left">空间数据处理</td> <td style="text-align: left">矢量数据读写、坐标转换、空间运算（交集、缓冲区）</td> </tr> <tr> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">matplotlib</code></td> <td style="text-align: left">静态可视化</td> <td style="text-align: left">绘制分级统计图、散点图、直方图等静态图表</td> </tr> <tr> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">folium</code></td> <td style="text-align: left">交互式地图</td> <td style="text-align: left">生成交互式 Web 地图，支持缩放、平移、点击查询</td> </tr> <tr> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">mapclassify</code></td> <td style="text-align: left">统计分级</td> <td style="text-align: left">提供自然断点法、分位数法等数据分级算法</td> </tr> <tr> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">jupyterlab</code></td> <td style="text-align: left">开发环境</td> <td style="text-align: left">代码执行、Markdown 文档、可视化预览</td> </tr> <tr> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">ipykernel</code></td> <td style="text-align: left">Jupyter 内核</td> <td style="text-align: left">连接 Python 虚拟环境与 Jupyter Notebook</td> </tr> <tr> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">geodatasets</code></td> <td style="text-align: left">示例数据集</td> <td style="text-align: left">提供纽约市行政区等空间分析示例数据</td> </tr> </tbody> </table> <p><strong>技术栈关系</strong>：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>geopandas (空间数据处理)
    ├── matplotlib (静态可视化)
    ├── folium (交互式可视化)
    └── mapclassify (统计分级算法)

jupyterlab (开发环境容器)
</code></pre></div></div> <p><strong>注意</strong>：虚拟环境需要在激活状态下才能通过 <code class="language-plaintext highlighter-rouge">uv add</code> 安装依赖包。</p> <h4 id="step-3-验证环境">Step 3: 验证环境</h4> <p>在终端输入以下命令启动 Jupyter Lab 服务，若浏览器自动弹出并显示界面，即表示配置成功：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv run jupyter lab
</code></pre></div></div> <hr/> <h2 id="第二部分课堂实操-notebook-in-class-lab">第二部分：课堂实操 Notebook (In-class Lab)</h2> <p><strong>文件名</strong>：<code class="language-plaintext highlighter-rouge">Lab1_Data_Structure_and_Visualization.ipynb</code></p> <p><strong>教学目标</strong>：在 VS Code 中运行 Jupyter Notebook，理解矢量数据结构，实现静态与交互式地图渲染。</p> <h3 id="1-环境连接与库导入-setup">1. 环境连接与库导入 (Setup)</h3> <p><strong>操作提示</strong>： 打开 <code class="language-plaintext highlighter-rouge">.ipynb</code> 文件后，点击右上角的 <strong>Select Kernel</strong>。在弹出的选项中，务必选择 <strong>Python Environments</strong> 下带有 <code class="language-plaintext highlighter-rouge">.venv</code> 标识的选项（这是 uv 创建的隔离环境）。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 导入空间数据处理核心库
</span><span class="kn">import</span> <span class="n">geopandas</span> <span class="k">as</span> <span class="n">gpd</span>

<span class="c1"># 导入绘图库，用于解决潜在的中文显示问题（可选）
</span><span class="kn">import</span> <span class="n">matplotlib.pyplot</span> <span class="k">as</span> <span class="n">plt</span>

<span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">Environment loaded successfully.</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <h3 id="2-数据加载-data-loading">2. 数据加载 (Data Loading)</h3> <p><strong>注意</strong>：GeoPandas 1.0+ 已移除内置数据集。使用 <code class="language-plaintext highlighter-rouge">geodatasets</code> 包获取示例数据：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 导入 geodatasets 包
</span><span class="kn">import</span> <span class="n">geodatasets</span>

<span class="c1"># 读取纽约市5个行政区边界数据
# nybb = New York Borough Boundaries
</span><span class="n">nybb</span> <span class="o">=</span> <span class="n">gpd</span><span class="p">.</span><span class="nf">read_file</span><span class="p">(</span><span class="n">geodatasets</span><span class="p">.</span><span class="nf">get_path</span><span class="p">(</span><span class="sh">'</span><span class="s">nybb</span><span class="sh">'</span><span class="p">))</span>

<span class="c1"># 检查数据加载状态
</span><span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Data CRS: </span><span class="si">{</span><span class="n">nybb</span><span class="p">.</span><span class="n">crs</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Shape: </span><span class="si">{</span><span class="n">nybb</span><span class="p">.</span><span class="n">shape</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>  <span class="c1"># 5个行政区
</span><span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Columns: </span><span class="si">{</span><span class="n">nybb</span><span class="p">.</span><span class="n">columns</span><span class="p">.</span><span class="nf">tolist</span><span class="p">()</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>

<span class="c1"># 查看行政区名称
</span><span class="nf">print</span><span class="p">(</span><span class="n">nybb</span><span class="p">[[</span><span class="sh">'</span><span class="s">BoroName</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">BoroCode</span><span class="sh">'</span><span class="p">]].</span><span class="nf">head</span><span class="p">())</span>
</code></pre></div></div> <p>本课程使用 <strong>纽约市行政区数据 (nybb)</strong> 进行演示，数据量小且属性完整。</p> <h3 id="3-核心数据结构解析-the-geodataframe">3. 核心数据结构解析 (The GeoDataFrame)</h3> <p>空间分析的核心在于理解 <strong>GeoDataFrame</strong> 结构。它在普通 DataFrame 的基础上增加了一个几何列。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 查看前 5 行数据
</span><span class="n">nybb</span><span class="p">.</span><span class="nf">head</span><span class="p">()</span>
</code></pre></div></div> <p><strong>关键技术点解析</strong>：</p> <ul> <li><strong>Attribute Columns (属性列)</strong>：如 <code class="language-plaintext highlighter-rouge">BoroName</code> (行政区名), <code class="language-plaintext highlighter-rouge">BoroCode</code> (行政区代码)，与常规表格数据无异</li> <li><strong>Geometry Column (几何列)</strong>： <ul> <li>这是 GeoDataFrame 的核心</li> <li>存储格式通常为 WKT (Well-Known Text) 对象，如 <code class="language-plaintext highlighter-rouge">POLYGON ((...))</code></li> <li>空间操作（如计算面积、求交集）均基于此列进行</li> </ul> </li> </ul> <h3 id="4-静态可视化-static-visualization">4. 静态可视化 (Static Visualization)</h3> <p>基于 <code class="language-plaintext highlighter-rouge">matplotlib</code> 引擎的快速制图，用于数据探索。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 基础绘图：直接映射 Geometry 列
</span><span class="n">nybb</span><span class="p">.</span><span class="nf">plot</span><span class="p">()</span>

<span class="c1"># 按行政区名称着色（类似分级统计图）
# column: 指定用于着色的属性列（此处为行政区名）
# legend: 显示图例
# figsize: 指定画布尺寸 (英寸)
</span><span class="n">nybb</span><span class="p">.</span><span class="nf">plot</span><span class="p">(</span><span class="n">column</span><span class="o">=</span><span class="sh">'</span><span class="s">BoroName</span><span class="sh">'</span><span class="p">,</span> <span class="n">legend</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span> <span class="n">figsize</span><span class="o">=</span><span class="p">(</span><span class="mi">10</span><span class="p">,</span> <span class="mi">6</span><span class="p">),</span> <span class="n">cmap</span><span class="o">=</span><span class="sh">'</span><span class="s">Set3</span><span class="sh">'</span><span class="p">)</span>
</code></pre></div></div> <h3 id="5-交互式可视化-interactive-visualization">5. 交互式可视化 (Interactive Visualization)</h3> <p>基于 <code class="language-plaintext highlighter-rouge">folium</code> (Leaflet.js) 的 Web 地图渲染。这对应了课件中提到的 <strong>LBS (Location-Based Services)</strong> 的前端展现形式。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 使用 .explore() 方法生成交互式地图
# 该方法会自动处理坐标投影转换，生成 Leaflet 地图对象
</span><span class="n">m</span> <span class="o">=</span> <span class="n">nybb</span><span class="p">.</span><span class="nf">explore</span><span class="p">(</span>
    <span class="n">column</span><span class="o">=</span><span class="sh">'</span><span class="s">BoroName</span><span class="sh">'</span><span class="p">,</span>          <span class="c1"># 按行政区名着色
</span>    <span class="n">tooltip</span><span class="o">=</span><span class="p">[</span><span class="sh">'</span><span class="s">BoroName</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">BoroCode</span><span class="sh">'</span><span class="p">],</span> <span class="c1"># 悬浮显示信息
</span>    <span class="n">cmap</span><span class="o">=</span><span class="sh">'</span><span class="s">Set3</span><span class="sh">'</span><span class="p">,</span>                <span class="c1"># 配色方案
</span>    <span class="n">tiles</span><span class="o">=</span><span class="sh">'</span><span class="s">CartoDB positron</span><span class="sh">'</span>    <span class="c1"># 底图样式
</span><span class="p">)</span>

<span class="c1"># 渲染地图对象
</span><span class="n">m</span>
</code></pre></div></div> <p><strong>操作</strong>：</p> <ul> <li>尝试缩放和平移地图</li> <li>观察这种可视化方式与静态图片的区别（更适合展示多尺度空间细节）</li> </ul> <h3 id="6-总结-summary">6. 总结 (Summary)</h3> <p>本节课我们完成了现代空间分析环境的搭建，并验证了从数据读取到交互式可视化的完整链路。</p> <p><strong>Next Step</strong>：</p> <p>请确保你的 VS Code 能成功运行上述代码，并熟悉 Jupyter Notebook 的基本操作（Cell 的执行与新建），为下周深入的数据清洗课程做准备。</p>]]></content><author><name></name></author><category term="空间分析"/><category term="环境配置"/><category term="VSCode"/><category term="uv"/><category term="Python"/><category term="GeoPandas"/><category term="空间分析"/><summary type="html"><![CDATA[第一部分：课前技术博客 (Pre-class Technical Blog)]]></summary></entry></feed>